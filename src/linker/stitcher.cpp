#include <vector>
#include <map>
#include <string>

#include "../../include/linker/stitcher.h"
#include "../../include/parsing.h"

using namespace std;

vector<string> stitch_program(vector<string> main, map<string, vector<string>> secondary_filepaths) {
    // TODO: Takes a main program and places contents of other files into it based on directives.
    vector<string> stitched;

    return stitched;
}